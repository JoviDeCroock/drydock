# E2E staged-tarball digest fixture

The stage record reports a digest the staged tarball's bytes do not hash to, so
the review must refuse to describe the artifact it downloaded as the staged
release.
